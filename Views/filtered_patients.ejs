<!DOCTYPE html>
<html>

<head>
    <title>Filtered Patients</title>
</head>

<body>
    <h1>Patients with First Name: <%= firstName %>
    </h1>
    <ul>
        <% patients.forEach(patient=> { %>
            <li>
                <strong>ID:</strong>
                <%= patient.patient_id %>,
                    <strong>Name:</strong>
                    <%= patient.first_name %>
                        <%= patient.last_name %>,
                            <strong>Date of Birth:</strong>
                            <%= patient.date_of_birth %>
            </li>
            <% }); %>
    </ul>
</body>

</html>